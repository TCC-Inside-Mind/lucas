function pagar() {
    // Simulação de pagamento com PIX (poderia ser uma chamada para uma API real)
    setTimeout(() => {
        // Exibir mensagem de confirmação após 2 segundos
        document.getElementById('confirmacao').style.display = 'block';
    }, 2000);
}
